#include <stdio.h>
#include "gen.h"
#include "view.h"

#define MAP_PATH "../ressources/conf/map.cluedo"
#define MAP_SIZE 24

int main(int argc, char* argv[]) {
  char** mymap = map(MAP_PATH, MAP_SIZE) ;
  for (int i = 0 ; i < MAP_SIZE ; i++)
    printf("%s\n", *(mymap + i)) ;
  launch_window(&argc, &argv, MAP_SIZE) ;
}
