#include "word.h"
#include "wordlist.h"

int add_to_index (Word word, Word url)
{
  char* tmpw = get_cstring_word(word) ;
  char* tmpu = get_cstring_word(url) ;
  printf("(%s, %s) added\n") ;
  return 0 ;
}

int peek_from_index (Word word, WordList* pages)
{
  return 0 ;
}
