from math import sqrt

IMG_MAX = 1200, 630
LOGO_PATH = "pcf2.png"
# OPACITY = 204
PHI = (1+sqrt(5))/2
FONT_PATH="fonts/Ubuntu-B.ttf"
INIT_FONTSIZE=10
INIT_TEXT=""
TEXT_RATIO = 30
LOGO_RATIO = 10
OPACITY = (0, 0, 0, 230)
TRANSPARENCY = (255, 255, 255, 0)

def minimize_image(img, maxsize):
    size = img.size
    while size[0] > maxsize[0] or size[1] > maxsize[1] :
        print("size:"+str(size)+" maxsize:"+str(maxsize))
        if size[0] > maxsize[0] : size = maxsize[0], size[1]
        elif size[1] > maxsize[1] : size = size[0], maxsize[1]
    img.thumbnail(size)
