#include <stdio.h>
#include <string.h>

char* lecture() {
  size_t taille = sizeof(char) ;
  size_t compte = 0 ;
  char* res = malloc(taille) ;
  
    while (tmp != EOF) {
      compte++ ;
      if (compte >= taille) {
	realloc(res, (void*) taille*2) ;
	taille *= 2 ;
      }
      res[compte-1] = getchar() ;
    }
    if (compte >= taille) 
      realloc(res, (void*) taille*2) ;
    res[compte] = '\0' ;
    return res ;
}

int main() {
  char* string = lecture() ;
  printf("%s\n",string) ;
}

