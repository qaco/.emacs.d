package protocol.feedback ;

import protocol.field.Field ;
import client.src.ServerRequest;
import client.src.ConnexionRequest;
import main.MainVals ;

public class DelFriendNO implements Feedback {

    public void exec(ServerRequest c) {
	System.out.println("[Vous n'êtes pas amis.]");
    }
    public void exec(ConnexionRequest cr) {}

    public byte[] toBytes() {
        return Field.makeSentence(FeedbackVals.DEL_OK, new Field[0]) ;
    }
    
    public String toString() {
        return FeedbackVals.DEL_OK + MainVals.EOF ;
    }
}
