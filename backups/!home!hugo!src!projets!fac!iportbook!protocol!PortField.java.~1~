package protocol ;

import exception.RuleBrokenException ;

import java.nio.charset.Charset ;

public class PortField extends Field {

    public PortField(byte[] myport) throws RuleBrokenException {
        super(myport) ;
    }

    public PortField(int myport) {
        try {
            super(String.valueOf(myport)) ;
        } catch (RuleBrokenException e) {
            throw new IllegalArgumentException() ;
        }
    }

    public getInt() {
        return Integer.valueOf(this.toString()) ;
    }
    
    protected void checkRules() throws UnknownResultException {
        try {
            int val = Integer.valueOf(this.toString()) ;
        } catch (NumberFormatException
    }
        
