OC=ocamlc
LINKS=graphics.cma
EXEC=coloriage
SRC=examples.ml sat_solver.ml voronoi.ml interaction.ml main.ml 
OBJ=$(SRC:.ml=.cmx)

all: $(EXEC)

$(EXEC) : $(OBJ)
	$(OC) -o $@ $(LINKS) $^

%.cmx: %.mli %.ml
$(CAMLC) -c $^

%.cmx: %.ml
$(CAMLC) -c $^

clean :
	rm -rf *.cmo *.cmi

mrproper : clean
	rm -rf $(EXEC)
