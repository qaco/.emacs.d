CC=gcc
CFLAGS=-std=gnu99 -Wall -g
EXEC=nim
SRC=nim.c
OBJ=$(SRC:.c=.o)

all : $(EXEC)

main : $(OBJ)
	$(CC) -o $@ $^

%.o: %.c
	$(CC) $(CFLAGS) -o $@ -c $<

check-syntax:
	gcc -o nul -S ${CHK_SOURCES}

clean :
	rm -rf *.o

mrproper : clean
	rm -rf $(EXEC)
