#include <stdio.h>

int main() {
  int a, b, c, *p1, *p2 ;
  a = 1 ;
  b = 2 ;
  c = 3 ;
  printf("a = %d, b = %d, c = %d,", &a, &b, &c) ;
  printf("p1 = %d, *p1 = %p, p2 = %d, *p2 = %p", &p1, p1, &p2, p2) ;
}
