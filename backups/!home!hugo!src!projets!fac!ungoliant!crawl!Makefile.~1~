SOURCES = crawler.c
OBJS = $(SOURCES:%.c=%.o)
CC = gcc
CFLAGS = -std=gnu99 -Wall -Werror -g


all : $(OBJS)

%.o : %.c
	$(CC) $(CFLAGS) -c $<

crawler.o : crawler.h ../misc/word.h ../misc/wordlist.h ../parse/parser.h ../db/db_manager.h

test : 

test.o : 

clean : 	
	rm -f $(OBJS)
