public class SomeMessage {

    private String mess ;

    public SomeMessage (String mymess) {
        if (mymess.length() <= 200)
            this.mess = mymess ;
        else
            this.mess = mymess.substring(0, 200) ;
    }

    public String getRepresentation() {
        return "FLOO? " + this.mess + "+++" ;
    }
}
