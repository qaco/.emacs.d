#include <gtk/gtk.h>

/* Fonction-test de remplissage de la stack
#define DESSIN "../ressources/images/dessin.png"
static void gribouille (GtkWidget* stack) {
  GdkPixbuf* pix_dessin  = gdk_pixbuf_new_from_file (DESSIN, NULL) ;
  GtkWidget* im_dessin = gtk_image_new_from_pixbuf(pix_dessin) ;
  gtk_overlay_add_overlay ((GtkOverlay*)stack, im_dessin);
  gtk_widget_show(im_dessin) ;
  } */
  

gboolean cb_click_on_tile (GtkWidget *event_box,
			   GdkEvent  *event,
			   gpointer data) {
  int x,y ;
  GtkWidget
    *stack = gtk_widget_get_parent (event_box),
    *grid = gtk_widget_get_parent(stack) ;
  GValue gval = G_VALUE_INIT ;

  g_value_init (&gval, G_TYPE_INT);
  
  gtk_container_child_get_property ((GtkContainer*)grid,
				    stack,
				    "left-attach",
				    &gval);
  x = g_value_get_int (&gval);
  gtk_container_child_get_property ((GtkContainer*)grid,
				    stack,
				    "top-attach",
				    &gval);
  y = g_value_get_int (&gval);
  
  printf("%d,%d,%c\n", x,y,*((char*)data)) ;

  return TRUE ;
}
