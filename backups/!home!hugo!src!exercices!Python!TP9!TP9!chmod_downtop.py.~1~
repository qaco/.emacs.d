#!/usr/bin/env python3

import os
import sys

if __name__ == "__main__" :
    for rout, dirs, files in os.walk(sys.argv[1]) :
        for f in files :
            print(f)
            os.chmod(os.path.abspath(os.path.join(rout,f)), int(sys.argv[2], 8))
