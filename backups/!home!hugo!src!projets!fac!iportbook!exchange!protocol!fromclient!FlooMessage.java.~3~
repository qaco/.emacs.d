package protocol.fromclient ;
import protocol.* ;
public class FlooMessage implements Protocol {

    private String mess ;

    public FlooMessage (String mymess) {
        if (mymess.length() <= 200)
            this.mess = mymess ;
        else
            this.mess = mymess.substring(0, 200) ;
    }

    public String toString() {
        return "FLOO? " + this.mess + "+++" ;
    }
}
