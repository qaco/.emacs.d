#include <check.h>
#include "word_test.h"


Suite *word_suite(void) {
  TCase *tc_word ;
  Suite *s ;

  tc_word = word_case() ;

  s = suite_create("All") ;
  suite_add_tcase(s, tc_word) ;

  return s ;
}

int main() {
  Suite* s = word_suite() ;
  SRunner* runner ;

  runner = srunner_create(s) ;

  srunner_run_all(runner, CK_NORMAL) ;

  srunner_free(runner) ;

  return 0 ;
}
  
  
  
