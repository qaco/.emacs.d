(* Ex 1 *)
let carre x = x*x ;;
let perimetre r = (let pi = 3.14159267 in (2.0*.pi*.r)) ;;
let bis str = str^str ;;
let huit_fois un = (let deux = un^un in (deux^deux)) ;;

(* Ex 2 *)
let est_nul i = i = 0 ;;
(* Ex 3 *)
let msg_nul i = if (i = 0) then ("nul") else ("non nul") ;;
(* Ex 4 *)
let mymax i j = if (i > j) then (i) else (j) ;;
let rec maxtriple a b c = max (max a b) c ;;
(* Ex 7 *)
let rec somme n = if (n = 0) then (0) else (n + (somme (n-1))) ;;
(* Ex 8 *)
let rec fibo n = if (n = 0 || n = 1) 
		 then (1) 
		 else (fibo(n-1) + fibo(n-2)) ;;
(* Ex 9 *)
let rec ea n = if (n = 0) then (0) else (1+(eb(n-1)))
and eb n = if (n = 0) then (0) 
	   else if ((n mod 2) = 0) then (1+(ea(n-2)))
	   else (1+(ea(n-1))) ;;
(* Ex 10 *)
let rec ack m n = if (m == 0) then (n+1)
		  else if (n == 0) then (ack (m-1) 1)
		  else (ack (m-1) (ack m (n-1))) ;;
  
