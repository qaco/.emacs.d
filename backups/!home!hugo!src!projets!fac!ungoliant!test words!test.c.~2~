#include <string.h>
#include "wordlist.h"
#include "word.h"

int main()
{
  /* Test de Word */
  Word myword = construct_word("bisou", strlen("bisou")) ;
  print_word(myword, stdout) ;
  if (!is_null_word(myword))
    printf(" n'est pas vide\n") ;
  add_char_word(&myword, 'x') ;
  insert_char_word(&myword, 'o', 3) ;
  remove_char_word(&myword, 1) ;
  print_word(myword, stdout) ;
  printf("\n") ;
  destruct_word(&myword) ;

  /* Test de WordList */
  Word gword = construct_word("bisou", strlen("bisou")) ;
  Word dword = construct_word("baba", strlen("baba")) ;
  Word oword = construct_word("hello", strlen("hello")) ;
  WordList glist = build_wordlist(gword) ;
  WordList dlist = build_wordlist(dword) ;
  WordList olist ;

  printf("\n") ;
  printf("Test append\n") ;
  append_wordlist(&glist, &dlist) ;
  print_wordlist(glist,stdout) ;

  printf("\n") ;
  printf("Test clone\n") ;
  olist = clone_wordlist(glist) ;
  print_wordlist(olist,stdout) ;

  printf("\n") ;
  printf("Test set\n") ;
  set_word_wordlist(&glist, 1, oword) ;
  print_wordlist(glist,stdout) ;
  printf("\n") ;

  printf("Test pop\n") ;
  oword = pop_word_wordlist(&glist) ;
  print_word(oword, stdout) ;
  print_wordlist(glist, stdout) ;

}
