#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* lecture() {
  size_t taille = sizeof(char) ;
  size_t compte = 0 ;
  char* res = malloc(taille) ;
  
    do {
      compte++ ;
      if (compte >= taille) {
	res = realloc((void*) res, taille*2) ;
	taille *= 2 ;
      }
      res[compte-1] = getchar() ;
    } while (res[compte-1] != EOF) ;
    return res ;
}

void ecriture(char* ptr) {
  while(*ptr != EOF) {
    printf("%c",*ptr) ;
    ptr++ ;
  }
}

void traitement(char* ptr) {

int main() {
  char* pgm_lu = lecture() ;
  ecriture(pgm_lu) ;
  free(pgm_lu) ;
}

