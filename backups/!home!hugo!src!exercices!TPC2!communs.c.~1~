#include <stdio.h>

void communs(int tab1[], int tab2[], int taille) {
  for (int i = 0 ; i < taille ; i++) {
    for (int j = 0 ; j < taille ; j++) {
      if (tab[i] == tab[j]) {
	printf("%d", tab[i]) ;
	break ;
      }
    }
  }
}

int main() {
  int tab1[] = {5, 9, 98, 26, 1} ;
  int tab2[] = {2, 9, 4, 26, 1} ;

}
