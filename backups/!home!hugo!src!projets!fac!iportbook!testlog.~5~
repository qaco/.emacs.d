
TODO

- Recevoir les messages en un seul coup

AFFICHAGE

V Lorsqu'un utilisateur reçoit une notification, il perd le symbole >
  symbolisant le prompt (corrigé)

TROUBLESHOOTING

V Le serveur tente de binder un port déjà sous écoute
  Message d'erreur et quit

V Crash d'un client : le serveur tient bon

V Crash du server : le client fait une stacktrace dégueulasse

EN FONCTION DU PROTOCOLE

que se passe t'il si le serveur ne respecte pas le protocole ?
que se passe t'il si le client ne respecte pas le protocole ?

EN FONCTION DE LA SAISIE UTILISATEUR

X Remarque générale : lorsqu'une commande est appelée avec des valeurs
  interdites, son constructeur envoie une RuntimeException : il faut donc
  tester les valeurs reçues de l'utilisateur AVANT d'appeler le constructeur
  (c'est fait pour certains, comme register, mais pas pour d'autres, comme
  message)

register
========

V id <= 8 cars && port <= 9999 && mdp <= 65535
  reponse positive

V id > 8 cars
  nouveau prompt

V mdp non-numeric
  nouveau prompt

V mdp > 65535

V problème : le client ne ferme pas la socket utilisée pour register
  et il en utilise une autre pour la connexion

? mdp < 0

connexion
=========

V id <= 8 cars && mdp < 65XXX && match
  reponse positive

V mdp > 65XXX
  nouveau prompt

V mdp non-numeric
  nouveau prompt

X !match
  le client dit "inscription refusée" et non connexion

X id > 8 cars
  le client affiche un test interne avant de revenir au prompt

? mdp < 0

messages
========

V id <= 8 cars && mess <= 200 cars && friend
  reponse positive
  stockage d'un flow


V !friend
  reponse negative

X mess > 200
  traité par le serveur mais le client freeze au moment de lire les flows

X id > 8 cars
  RuntimeException coté client


demande d'amitié
================

V id <= 8 cars && exists
  reponse positive

V !exists
  reponse negative

X id > 8 cars
  RuntimeException coté client

consultation
============

V hasflows

V hasnoflows

list
====

V alone

V others

flood
=====

V mess <= 200 cars

V mess > 200 cars
  

deconnexion
===========

X reconnexion après une déconnexion : le client dit qu'il y a une erreur

notificaiton
============

X gérer message quand fermeture brutale qui interrompt les notifs / changer le message à afficher