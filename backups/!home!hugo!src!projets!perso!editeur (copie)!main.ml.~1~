open Graphics ;;
open Fieldtext ;;
open Layman ;;

  open_graph " 900x900" ;;
  let mycont = container_build (0,0) (900,800) ;;
  let myfield = fieldtext_build (400,400) 50 "bisou" ;;
  let mynewcont = add_comp mycont (Fieldtext(myfield)) ;;
    draw_container mynewcont ;;
      ignore (container_interaction mynewcont) ;;
        close_graph ;;
