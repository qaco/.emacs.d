#include <stdio.h>
#define INF 33
#define SUP 126

int char* generation (int taille) {
  char res[taille + 1] ;
  srand(time(NULL)) ;
  for (int i = 0 ; i < taille ; i++)
    res[i] = rand() % (SUP - INF) + INF ;
  res[taille + 1] = '\0' ;
}

int main() {
  printf("%s\n", generation(10)) ;
}
