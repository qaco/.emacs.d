#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>


int main(int argc, char* argv[]) {
  char* pseudo = malloc(sizeof(char)*1000) ;
  int server = mkfifo("patron", S_IRWXU | S_IRWXG | S_IRWXO) ;
  read(server, pseudo, 4) ;
  *(pseudo + 4) = '\0' ;
  printf("%s\n", pseudo) ;
}
