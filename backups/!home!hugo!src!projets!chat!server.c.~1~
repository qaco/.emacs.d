int main(int argc, char* argv) {
  char* pseudo = malloc(sizeof(char)*1000) ;
  int server = mkfifo("patron", S_IWUSR) ;
  read(server, pseudo, 4) ;
  *(server + 4) = '\0' ;
  printf("%s\n", pseudo) ;
}
