type seed =
  {c : color option;
   x : int;
   y : int}
;;

type voronoi = { dim : int * int; seeds : seed array };;

module type Builder =
  sig
    val regions_voronoi : int -> int -> int -> int -> voronoi
                          ->  int array array
    val adjacences_voronoi : voronoi -> int array array
                             -> bool array array
  end
;;
      
