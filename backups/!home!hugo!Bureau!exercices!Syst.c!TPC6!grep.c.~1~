#include <stdio.h>

#define LIGNE_MAX 80

int main(int argc, char* argv[]) {

  argv++ ;
  argc-- ;
  char* expression, entree, sortie ;

  switch(argc) {
  case 2 :
    expression = *argv ;
    entree = *argv + 1 ;
  case 3 :
    sortie = *argv + 2 ;
    break ;
  default :
    exit(EXIT_FAILURE) ;
    break ;
  }

  FILE* cobaye = fopen(entree, "r") ;
}
