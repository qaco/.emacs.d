02/03/2017
==========

Décisions :
- Architecture du Main : paramétrage par la ligne de commande
- Architecture des tests : Un makefile

À faire pour la semaine suivante :
- Corriger le bug bloquant dans add_word_wordlist (Hugo)
- Paramétrage de la profondeur du crawl (Hugo)
- Query engine (Guillaume)
- L'instanciation de la wordlist se fait dans le crawler (Hugo)
- Main (Hugo)
- Makefile des tests (Vincent)

